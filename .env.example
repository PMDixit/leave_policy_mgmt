# Django Configuration
# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY=django-insecure-dev-key-change-in-production-123456789012345678901234567890

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG=True

# Comma-separated list of allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Configuration
# For development, you can use SQLite (no additional config needed)
# For production/staging, configure PostgreSQL settings below
DB_NAME=leave_policy_db
DB_USER=postgres
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432

# Tenant Service Configuration
# URL of the external tenant/user authentication service
TENANT_SERVICE_URL=https://tenants1.devapitenant.techforing.com

# Optional: Django Settings Module
# Can be set to override default settings module
# DJANGO_SETTINGS_MODULE=config.settings.development

# CORS Configuration
# Comma-separated list of allowed origins for CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8080,http://127.0.0.1:8080

# Allow credentials in CORS requests
CORS_ALLOW_CREDENTIALS=True

# CSRF Configuration
# Comma-separated list of trusted origins for CSRF protection
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8080,http://127.0.0.1:8080

# Use secure CSRF cookie (should be True in production with HTTPS)
CSRF_COOKIE_SECURE=False
